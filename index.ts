function addNumbers(num1: number, num2: number): number {
    return num1 + num2;
}

function divNumbers(num1: number, num2: number): number {
    return num1 / num2;
}

console.log(addNumbers(5, 3)); // Outputs: 8
console.log(addNumbers(4, 2));
